import HomeExperience from '@/components/HomeExperience'
import { CURATION_APP_PATHS } from '@/lib/navigation/appPaths'

export default function CurationPage() {
  return <HomeExperience navigationPaths={CURATION_APP_PATHS} />
}
